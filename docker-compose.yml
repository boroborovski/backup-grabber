services:
  backup-grabber:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./backups:/data/backups
      - app-db:/data
      - ~/.ssh:/root/.ssh:ro
    restart: unless-stopped

volumes:
  app-db:
